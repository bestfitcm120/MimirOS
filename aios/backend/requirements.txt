# ── Web framework ──────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
python-dotenv==1.0.1

# ── Database ───────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.3
pgvector==0.3.5

# ── Validation ─────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2

# ── LLM — OpenRouter (uses openai SDK, pointed at OpenRouter) ──
openai==1.51.0

# ── Embeddings — local (no API key needed) ─────────────────
sentence-transformers==3.2.1
# sentence-transformers pulls torch automatically.
# If you want CPU-only torch (smaller download):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install sentence-transformers

# ── Embeddings — OpenAI direct (only if EMBEDDING_PROVIDER=openai) ──
# openai already listed above — no extra package needed.

# ── File processing ─────────────────────────────────────────
pypdf==4.3.1
python-docx==1.1.2
chardet==5.2.0
Pillow==10.4.0

# ── Task queue ──────────────────────────────────────────────
celery[redis]==5.4.0
redis==5.1.1

# ── File storage ────────────────────────────────────────────
minio==7.2.9

# ── AI agent framework ──────────────────────────────────────
langchain==0.3.1
langchain-openai==0.2.1
langgraph==0.2.28
tiktoken==0.8.0

# ── Utilities ───────────────────────────────────────────────
httpx==0.27.2
structlog==24.4.0
tenacity==9.0.0
numpy==1.26.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
gitpython==3.1.43
